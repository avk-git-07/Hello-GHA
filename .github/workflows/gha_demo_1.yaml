name: GHA Demo 1
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: demo_build_2
        run: echo "This is Demo build 2"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: demo_test_2
        run: echo "This is demo test 2"
  
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: demo_deploy_2
        run: echo "This is demo deploy 2"
